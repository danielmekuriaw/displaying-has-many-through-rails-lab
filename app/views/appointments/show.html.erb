<h2><%= link_to @appointment.patient.name, patient_path(@appointment.patient)%></h2>
<p><%= link_to @appointment.doctor.name, doctor_path(@appointment.doctor)%> </p>
<p> <%= @appointment.appointment_datetime.strftime("%B %d, %Y at %k:%M") %>