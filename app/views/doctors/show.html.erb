<h2><%= @doctor.name%></h2>
<p><%= @doctor.department%> </p>
<p><% @doctor.appointments.each do |appointment|%> </p>
<p> <%= link_to appointment.patient.name, patient_path(appointment.patient)%> - <%= appointment.appointment_datetime.strftime("%B %d, %Y at %k:%M") %>
<% end %>